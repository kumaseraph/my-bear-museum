# 熊熊博物館 v6.18 優化計畫

## 1. 星星按鈕優化
**需求：** 紀念熊星星按鈕要與其他熊熊一致（大小、位置）
- 大小：32px（目前約20px）
- 位置：左上方（top: 6px; left: 6px）
- 移除圓圈外框（無 border-radius: 50%、透明背景）

**檔案：** index.html
- `.star-btn` CSS 樣式調整

---

## 2. 縮圖外框優化
**需求：** 每個熊熊縮圖要有統一的外框效果
- 方案：新增 `.thumb-ring` 外框層（五彩漸層 + 旋轉動畫）
- 使用 `::before` 偽元素
- animation: ringRotate 4秒

**檔案：** index.html
- `.bear-thumb` 相對定位
- `.thumb-ring` 新增樣式

---

## 3. 各區塊星星顯示一致性
**問題：** 「新入住熊熊」區塊的星星與「年鑑」區塊的星星顯示方式不一致
- 需要檢查兩個區塊的 `.star-btn` 樣式是否統一
- 確認 `toggleStar()` 和 `updateStarButtons()` 邏輯是否一致

**檔案：** index.html
- 比較 `.bear-grid .star-btn` vs `.yearbook-grid .star-btn`
- 統一樣式和行為

---

## 4. 年鑑區塊圖示置換
**需求：** 熊熊年鑑的月份、特質等原本用 emoji 的圖示，改用自製的小熊圖示

**位置 A - 年鑑 Tab 切換：**
- `📅 熊熊月份` → 自製小熊圖示 + 文字
- `🎨 熊熊特質` → 自製小熊圖示 + 文字

**位置 B - 年鑑年份標題：**
- `📅 ${year}年` → 自製小熊圖示 + 文字

**位置 C - 年鑑月份/分類橫條：**
- `<span class="bar-arrow">▶</span>` → 自製小熊箭頭圖示

**檔案：** index.html
- `renderYearbookMonth()` 函數
- `renderYearbookCategory()` 函數
- 需要先確認已有哪些自製圖示可用（images/目錄）

---

## 預計版本：v6.18